<div class="row">
  <div class="col-sm-12">
    <div id="profile-header">
      <h3>
        <%= @organization.name %>
        <%= link_to '<span class="glyphicon glyphicon-edit"></span>
'.html_safe, edit_organization_path(@organization), class: "edit right" %>
      </h3>
      <% if @organization.main_address %>
        <span class="left"><%= @organization.main_address.address1 %>, Vancouver, <%= @organization.main_address.state %>, <%= @organization.main_address.zip %></span>
      <% end %>
      <p><%= @organization.headline %></p>
      <br /><br />
      <% if status == "active" %>
        <span class="label label-success left status"><%= status %></span>
      <% else %>
        <span class="label label-danger left status"><%= status %></span>
      <% end %>
    </div>
    <br /><br />
  </div>
</div>
