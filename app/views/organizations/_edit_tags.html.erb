<div class="row">
  <div class="panel panel-default">
    <div class="panel-body">
      <h3>Tags</h3>
      <table class="table table-striped tags-table">
        <% if @organization.tags.any? %>
          <tr>
            <td>
              <div class="tag-list">
                <% @organization.tags.each do |tag| %>
                  <span class="pull-left">
                    <%= link_to tag.name, search_path(tag: {names: [tag.name]}), class: "entity entity-tag" %>
                    <%= link_to '<span class="glyphicon glyphicon-remove"></span>
      '.html_safe, destroy_tag_path(@organization.id, tag.id), method: :delete, class: "remove-tag" %>
                  </span>
                <% end %>
              </div>
            </td>
          </tr>
        <% end %>
        <tr>
          <td>
            <%= render "organizations/tags_form" %>
          </td>
        </tr>
      </table>  
    </div>
  </div>
</div>
