<div id="tags">
  <div class="tag-list">
    <% @organization.tags.each do |tag| %>
      <%= link_to tag.name, search_path(tags: {names: [tag.name]}), class: "entity entity-tag pull-left" %>
    <% end %>
  </div>
</div>
