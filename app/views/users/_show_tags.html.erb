<div id="tags">
  <div class="tag-list">
    <% @user.tags.each do |tag| %>
      <%= link_to tag.name, search_path(tag: {names: [tag.name]}), class: "entity entity-tag pull-left" %>
    <% end %>
  </div>
</div>
