<div class="auth-btns right">
<% if current_user.nil? %>
  <script type="text/javascript">
    
    var widget = new Auth0Widget({
      domain:         '<%= Rails.application.secrets.auth0_username %>.auth0.com',
      clientID:       'N8s02JgamO99vbx7FOsyS6rJN8nbJMIl',
      callbackURL:    'http://localhost:3000/auth/auth0/callback'
    });
    
  </script>
  <button onclick="widget.signin({ scope: 'openid profile' })" class="btn btn-default" >Login</button>
<% end %>
<% if current_user %>
  <%= link_to "Logout", unset_session_path, class: "btn btn-default" %>
<% end %>
</div>
